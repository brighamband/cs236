#ifndef HEADER_H
#define HEADER_H

#include <string>
#include <vector>

class Header {
   private:
    std::vector<std::string> names;

   public:
    // Header(std::string n) {
    //     names.push_back(n);
    // }
    // string toString() const {
    //     return value;
    // }
};

#endif